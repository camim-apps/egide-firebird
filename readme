# Install Chocolately (como administrador)

$ Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
$ choco install -y nodejs --version=16.4.0
$ choco install -y git.install

# Instalar o PM2

$ npm i -g pm2

# Instalar o app de transferência

$ git clone https://github.com/camim-apps/egide-firebird.git .
$ cd egide-firebird
$ npm install

# Criar o arquivo de configuração

> Replicar o arquivo .env.example para .env que está dentro da pasta egide-firebird e atualizar

# Criar o banco local

$ npm run db:migrate

# Rodar o app

# Others
- Windows 10: shell:common startup - Criar um link para o start.bat e mover o link pra dentro da pasta
- Windows 7: Instalar o node 13

- https://www.centennialsoftwaresolutions.com/post/install-node-js-on-windows-7
- https://git-scm.com/download/win